apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jv-colored.fullname" . }}-test-connection"
  labels:
{{ include "jv-colored.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jv-colored.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
