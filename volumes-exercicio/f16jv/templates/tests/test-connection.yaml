apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "f16jv.fullname" . }}-test-connection"
  labels:
{{ include "f16jv.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "f16jv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
